import 'package:flutter/material.dart';

class TradingBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("TradingBody"),
    );
  }
}
